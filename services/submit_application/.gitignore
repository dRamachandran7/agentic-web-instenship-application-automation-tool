# This service reads search_listings' saved session — it doesn't capture its
# own — but ignore the pattern here too in case that ever changes.
.auth/
*.storage.json

# Contains the user's contact info / screening-question answers.
.profile/profile.json

# Local env
.env

# Python
__pycache__/
*.pyc
.venv/
venv/
